"""Outbound ports: interfaces the core needs from the outside world.

Examples: LedgerRepository, LedgerReadModel, AnnotationRepository,
StaffRepository, RoleRepository, PermissionResolver, CategoryRepository,
DonationAccountRepository, SettingsRepository, AuditLogRepository, Clock,
IdGenerator, UnitOfWork, BackupService. Implemented by ``adapters``.
"""
